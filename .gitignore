node_modules
.env.*
.env
docs
# Rendered marketing image variants (generated on demand)
src/images/marketingsite/.resized/

# Production secrets and local server output: never commit.
.env_prod
server.log
server.err.log
sandbox.log
sandbox.err.log

# A nested clone someone left here; never part of this repo.
serverpe-pravesha-back-end/
